repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: .venv/bin/pytest test_pricing_service.py -q
        language: system
        pass_filenames: false
        always_run: true
